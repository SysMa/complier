class test {
	//Test SyntaxErrors: Maybe you need to add the main function declaration like 'static void main()'.
	//void testSyntaxError() {
	static void main() {
		operateI operI <= new operateI();
		operateII operII <= new operateII();
		int [] x <= [2, 3];
		int a;
		int b;
		a <= operI.absoluteValue(x[0]);
		b <= operII.multiply(x[0], x[1]);
	}
}
class operateI {
	int absoluteValue(int x) {
		//Test SyntaxErrors: In front of the function declaration would be a pre-condition statement like 'pre : param > 4;'.
		pre: x < 0;
		//Test SyntaxErrors: There would be a post-condition statement like 'post : returnVal > 4;' after the pre-condition statement in front of the function declaration.
		post: y > 0;
		int y;
		y <= 0-x;
		return y;
	}
}
class operateII {
	int value <= 10;
	int multiply(int [] x) {
		pre: x.length > 1;
		post: x.length > 1;
		int mult;
		if(x[0] < 0) {
			int temp <= x[0] + value;
			mult <= temp*x[1];
		}
		else
			mult <= x[0]*x[1];
		//Test SyntaxErrors: Maybe you would add a return statement like 'return 3;' at the end of a method declaration.
		return mult;
	}
}